{
    "name": "brv/neucore-heroku",
    "description": "Only for Heroku to setup the PHP environment and install the backend and frontend.",
    "require": {
        "php": "^7.1.0",
        "ext-pdo_mysql": "*",
        "ext-zip": "*",
        "ext-mbstring": "*",
        "ext-intl": "*",
        "ext-dom": "*",
        "ext-curl": "*"
    },
    "scripts": {
       "post-install-cmd": [
            "./install.sh"
        ]
    }
}
